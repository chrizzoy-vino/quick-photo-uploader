DATABASE_URL="file:./data/db.sqlite"
STORAGE_ROOT="./data/albums"
# Zugangs-Secret fuer den Admin-Bereich (/admin) - langer, zufaelliger Wert, z.B. via
# `openssl rand -hex 32` erzeugen. Ohne gesetztes Secret bleibt der Admin-Bereich gesperrt.
ADMIN_SECRET=""
